import 'package:flutter/material.dart';

class CharactersItems extends StatelessWidget {
  const CharactersItems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}